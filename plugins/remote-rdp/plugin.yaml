apiVersion: runpilot.plugin/v1
id: remote.rdp
name: RDP / Guacamole
description: RDP remote access provider
version: 1.0.0
requires:
  runpilotApi: 1
capabilities:
  - type: remote-provider
    id: rdp
backend:
  module: backend/plugin.wasm
frontend:
  module: web/plugin.js
  stylesheet: web/plugin.css
permissions: [network, plugin-config]
